
<div class="header">

  <h3>Wikinimous</h3>
  <%= link_to "Create a new article", "articles/new" %>
</div>
<div class="container">

  <% @articles.each do | article | %>
    <div class = "paired">
      <h2><%= link_to article.title, article_path(article) %></h3>
      <h5><%= article.content %></h5>
    </div>
  <% end %>
</div>

